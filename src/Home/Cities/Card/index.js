import React from "react";
import styled from "styled-components";

const Card = styled.div`
  width: 509px;
  height: 293px;

  background: #ffffff;
  box-shadow: 0px 2px 12px rgba(0, 75, 93, 0.12);
  border-radius: 8px;
`;

export default function(props) {
  return <Card>afg</Card>;
}
